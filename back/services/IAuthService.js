class IAuthService {
  register(user) {
    throw new Error("Método não implementado!");
  }
  login(username, password) {
    throw new Error("Método não implementado!");
  }
}

module.exports = IAuthService;
